import React from 'react';

import {
    ScrollView,
    Text,
} from 'react-native';

export default () => (
    <ScrollView>
        <Text>
            BREAKOUTS
        </Text>
    </ScrollView>
);
